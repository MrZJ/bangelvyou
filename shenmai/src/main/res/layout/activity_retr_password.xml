<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main_color"
    android:orientation="vertical">

    <include
        android:id="@+id/retr_password_top"
        layout="@layout/top_bar" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginLeft="26dp"
        android:layout_marginRight="26dp"
        android:layout_marginTop="50dp"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/edt_retr_password_inputcode"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="2"
            android:background="@drawable/bg_edittext"
            android:drawableLeft="@mipmap/messgae_code"
            android:drawablePadding="5dp"
            android:hint="输入短信验证码"
            android:numeric="integer"
            android:singleLine="true"
            android:textColorHint="#999999"
            android:textSize="12sp"
            android:textColor="#999999"/>

        <Button
            android:id="@+id/btn_retr_password_getcode"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@drawable/bg_button"
            android:layout_marginLeft="5dp"
            android:text="获取验证码"
            android:textSize="12sp"
            android:textColor="#FFFFFF" />

    </LinearLayout>

    <EditText
        android:layout_marginTop="18dp"
        android:id="@+id/edt_retr_password_newpassword"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginLeft="26dp"
        android:layout_marginRight="26dp"
        android:background="@drawable/bg_edittext"
        android:hint="输入新密码"
        android:numeric="decimal"
        android:singleLine="true"
        android:drawableLeft="@mipmap/login_secret"
        android:drawablePadding="5dp"
        android:textColor="#999999"
        android:textColorHint="#999999"
        android:textSize="12sp"
        android:inputType="textPassword"/>

    <Button
        android:id="@+id/btn_retr_password_commit"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginLeft="25dp"
        android:layout_marginRight="25dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/bg_button"
        android:text="提交"
        android:textColor="#FFF9FF"
        android:textSize="14sp" />

</LinearLayout>
